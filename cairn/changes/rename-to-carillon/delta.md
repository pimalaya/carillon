---
cairn: delta
change: rename-to-carillon
---

## ADDED Requirements

## MODIFIED Requirements

Nothing the spec requires changed. The daemon capability is renamed throughout, which is a change of name rather than of behaviour: the same watch, the same methods, the same hooks, under the name the tool now carries.

## REMOVED Requirements
