---
cairn: tasks
change: json-schema-output
---

- [x] Rename `CheckReport` to `CheckOutput` and `GeneratedConfig` to `ConfigureOutput`
- [x] Derive `JsonSchema` on both and on the rows they hold
- [x] Add `src/json_schema.rs` and the `json-schema` subcommand
- [x] Fold the delta into the spec and log the change
